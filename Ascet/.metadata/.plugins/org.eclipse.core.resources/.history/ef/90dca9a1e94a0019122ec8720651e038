package components;
import components.lib.average;

static class Component
reads veloCar.vfl
writes veloCar.avgVelCar {
	average average_instance;

	@generated("blockdiagram")
	public void calc() {
		veloCar.avgVelCar = average_instance.calc(veloCar.vfl, veloCar.vfl, veloCar.vfl, veloCar.vfl); // Main/calc 1
	}
}